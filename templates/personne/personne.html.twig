<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  
    <title>Persoone</title>
</head>
<body>
    {% for message in app.session.flashbag.get('error') %}
        <div style="color:red">
        {{message}}</div>
    {% endfor %}
        {% for message in app.session.flashbag.get('success') %}
        <div style="color:green">
        {{message}}</div>
    {% endfor %}
    {% for person in persons %}
    <a href="{{ path('detailPersonne',{id:person.id})}}">{{person.firstname}}</a>
    {% endfor %}

    <nav aria-label="Page navigation example">
  <ul class="pagination">
    {% if page !=1 %}
    <li class="page-item"><a class="page-link" href="{{path('personne',{page:page-1 , nbre:nbre})}}">Previous</a></li>
    {% endif  %}
    {% for i in range(1, nbrePage) %}
    <li class="page-item"><a class="page-link" href="{{path('personne',{page:i , nbre:nbre})}}">{{i}}</a></li>
    {% endfor %}
    {% if page !=nbrePage %}
    <li class="page-item"><a class="page-link" href="{{path('personne',{page:page+1 , nbre:nbre})}}">Next</a></li>
    {% endif %}
  </ul>
</nav>


    
</body>
</html>